version: 0.2


phases:
  install:
    runtime-versions:
      python: 3.7
  build:
    commands:
      - cd Discovery-PytestAutomation
      - pytest -s . --api --only-run-file "this doesn't exist, so every test is skipped..."
