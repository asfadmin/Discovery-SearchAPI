version: 0.2

env:
  variables:
    VENV: "search-api-venv"

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - python3.7 -m venv ${VENV}
      - . ./${VENV}/bin/activate
      - python3.7 -m pip install -r requirements.txt --upgrade
  build:
    commands:
      - cd Discovery-PytestAutomation
      - CMD="python3.7 -m pytest ${PYTEST_OPTIONS} . --api ${API_URL} ${TEST_SUITE_OPTIONS}"
      - echo -e "\nRunning command=> ${CMD}\n" # Get exact command into logs for debugging
      - ${CMD}
