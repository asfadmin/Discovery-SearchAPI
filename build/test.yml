version: 0.2

env:
  variables:
    VENV: "search-api-venv"

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      # - python -m venv ${VENV}
      - . ./${VENV}/bin/activate
  build:
    commands:
      - . ./${VENV}/bin/activate # It didn't exist here with the last run.... Do you have to reactivate it?
      - echo "testing..."
      - python -c "import sys;print(hasattr(sys, 'real_prefix'))" # Prints True/False to screen, if you're in a venv
      - command -v pytest # See if pytest exists
      - cd Discovery-PytestAutomation && python -m pytest -s --collect-only .
      # - cd Discovery-PytestAutomation && pytest #-s . --api prod --only-run-file "this doesn't exist, so every test is skipped..."
