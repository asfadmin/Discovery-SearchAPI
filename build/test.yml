version: 0.2

env:
  variables:
    VENV: "search-api-venv"

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - python -m venv ${VENV}
      - . ./${VENV}/bin/activate
      - pip install -r requirements.txt
  build:
    commands:
      - cd Discovery-PytestAutomation && python -m pytest -s --collect-only .
      # - cd Discovery-PytestAutomation && pytest #-s . --api prod --only-run-file "this doesn't exist, so every test is skipped..."
