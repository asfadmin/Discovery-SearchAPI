version: 0.2

env:
  parameter-store:
    build_ssh_key: "build_ssh_key"
  variables:
    ENV: "search-api"

phases:
  post_build:
    commands:
      - . ./${ENV}/bin/activate
      - zappa update ${MATURITY}
