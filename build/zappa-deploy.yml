version: 0.2

env:
  parameter-store:
    build_ssh_key: "build_ssh_key"
  variables:
    VENV: "search-api-venv"

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - python -m venv ${VENV}
      - . ./${VENV}/bin/activate
      # - pip install -r requirements.txt
  build:
    commands:
      - echo $0
      - exit -99999999999
      # - zappa update ${MATURITY}
      # # Note: the "${MATURITY##*_}" means everything after the last '_'.
      # - |
      #   if [[ "${MATURITY##*_}" == "staging" ]]; then
      #     if [[ -z "${API_URL}" ]]; then 
      #       echo -e "\nAPI_URL not set for staging! Quitting...\n"
      #       exit -1
      #     fi
      #   echo "running pytest suite here... eventually"
      #   fi
